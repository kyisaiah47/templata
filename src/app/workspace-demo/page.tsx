'use client';

import { AppShell } from '@/components/workspace/AppShell';

/**
 * Workspace Demo Page
 *
 * Full AppShell with all three workspace modes:
 * - Template: Guided, structured flow
 * - Reflection: Stillness and depth
 * - Life OS: Synthesis and perspective
 *
 * Features:
 * - Stage switcher in TopNav
 * - Keyboard shortcuts (g+t/r/l)
 * - Enhanced transitions
 * - Theme tokens per stage
 * - Right dock inspector
 */
export default function WorkspaceDemoPage() {
  return <AppShell />;
}
